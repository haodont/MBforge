# Archived TODO Items — 2026-07-29

Completed TODO items moved out of the active task board. The implementation
and verification details below preserve the audit trail without keeping these
items in `TODO/INDEX.md`.

| ID | Area | Completion evidence |
|---|---|---|
| M-1 | Quality baseline | Ruff `check` clean (last 10 errors fixed in markush tests); `ruff format` applied repo-wide (78 files, 0 drift remaining); ESLint 53 errors → 0 (PdfViewer ref-read refactor, strict-type fixes). Vendored-formatting boundary resolved by adopting `molscribe_inference` as first-party code with MIT attribution in the package docstring; lint `per-file-ignores` retained only for upstream-derived patterns. Also closes IMMEDIATE-ACTIONS #23 (Ruff style warnings) and #24 (CRLF→LF normalization, 15 backend files). |
| M-2 | Maintenance boundaries | Migration machinery removed (`core/migration.py`, `migrate_library.py`, 3 one-shot `scripts/migrate_*.py`, `test_migration.py`, legacy layout/crop resolvers, dead dual-DB branches and `_REVIEW_QUEUE_DDL`); `pipeline/organizer.py` (977 lines) split into `markdown_markers.py` / `molecode_insert.py` / `reorganize.py` / `persist_text_links.py`; `services/pdfService.ts` converged into `api/http/detection_cache.ts` (`extractPdfMolecules` / `savePageDetections`, `services/` deleted); stale phase labels and dangling TODO anchors (`§x.y`, `#P-3`, `#T-2`) purged across backend and frontend, including 3 user-visible strings. |
| D-11 | Documentation alignment | README seven-stage pipeline claim and research-baseline statement are now aligned with `docs/wiki/pipeline.md` and the project's review workflow (commit b4c1a8c). |

Verification at archive time: `uv run pytest tests/unit/ -q` 859 passed;
`ruff check src tests` and `ruff format --check` clean; frontend vitest
339 passed; `npx eslint src` 0 errors / 22 warnings (warnings out of scope);
frontend production build passed.
