/// <reference types="vite/client" />

// Injected by vite.config.ts from frontend/package.json `version` field
// at build time. Bump the version in package.json to roll it forward.
declare const __APP_VERSION__: string
